name: Run iShares Importer

on:
  workflow_dispatch:
    # Keine Inputs mehr definiert -> Das UI zeigt beim manuellen Start keine Felder an.
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      app_env:
        required: true
        type: string
      run_date:
        required: false
        type: string
    secrets:
      SUPABASE_URL:
        required: true
      SUPABASE_SERVICE_KEY:
        required: true
      SUPABASE_KEY:
        required: true

jobs:
  run-ishares-importer:
    runs-on: ubuntu-latest
    # Erkennung: Wenn per workflow_call aufgerufen, nimm die übergebenen Inputs.
    # Wenn manuell gestartet (workflow_dispatch), berechne die Werte anhand des Branches.
    environment: >-
      ${{ github.event_name == 'workflow_call' && inputs.environment || 
          (github.ref_name == 'dev' && 'test' || 'production') }}
    env:
      APP_ENV: >-
        ${{ github.event_name == 'workflow_call' && inputs.app_env || 
            (github.ref_name == 'dev' && 'dev' || 'main') }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run iShares Importer
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        shell: bash
        run: |
          run_date="${{ github.event_name == 'workflow_call' && inputs.run_date || '' }}"
          if [ -z "$run_date" ]; then
            run_date="$(date -u +%F)"
          fi
          export RUN_DATE="$run_date"

          summary_dir="${{ runner.temp }}/nightbatch-ishares-summary"
          mkdir -p "$summary_dir"
          log_file="$summary_dir/script_output.log"
          metadata_file="$summary_dir/metadata.json"
          start_time="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          start_epoch="$(date +%s)"

          set +e
          python - <<'PY' 2>&1 | tee "$log_file"
          from src.nightbatch.ishares_update import process_all_ishares_assets
          process_all_ishares_assets(dry_run=False)
          PY
          command_status=${PIPESTATUS[0]}
          set -e

          finish_time="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          finish_epoch="$(date +%s)"
          duration_seconds="$((finish_epoch - start_epoch))"
          status="success"
          if [ "$command_status" -ne 0 ]; then
            status="failure"
          fi

          cat > "$metadata_file" <<EOF
          {
            "workflow_key": "ishares",
            "workflow_name": "iShares Import",
            "status": "$status",
            "start_time": "$start_time",
            "finish_time": "$finish_time",
            "duration_seconds": $duration_seconds,
            "log_file": "script_output.log"
          }
          EOF

          if [ "$command_status" -ne 0 ]; then
            exit "$command_status"
          fi

      - name: Upload iShares Summary Artifact
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: nightbatch-ishares-summary
          path: ${{ runner.temp }}/nightbatch-ishares-summary
          if-no-files-found: error